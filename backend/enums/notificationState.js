//notificationState.js

const NotificationState = Object.freeze({
    SENT: "Sent",
    RECEIVED: "Received",
    SEEN: "Seen",
    INITIAL: "Initial",
    ERROR: "Error"
  });

module.exports =  NotificationState;